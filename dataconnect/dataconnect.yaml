specVersion: "v1"
serviceId: "pizzeria-nostra"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "pizzeria-nostra-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
